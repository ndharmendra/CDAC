# program that accepts a sequence of whitespace separated words as input and# prints the words after removing all duplicate words and sorting# them alphanumericallyprogram that accepts a sequence of whitespace# separated words as input and prints the words after removing all duplicate# words and sorting them alphanumericallydef my_function(x):    return list(dict.fromkeys(x))lst = [x for x in input().split(" ")]lst.sort()print(my_function(lst))